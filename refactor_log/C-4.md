Module 去 VM 耦合完成后，按 module_refactor.md 进入：

1. PyModule.hpp — 添加 KeyVersionTracker + dict_version（为 inline cache 准备）
2. RuntimeContext.cpp — 考虑移除最后的 VM.hpp 依赖（stack_local 降级路径已被 set_stack_local_provider 覆盖，可改为纯 assert 失败）
3. 测试验证 — 全量运行 ctest，确保所有 integration test 通过